import React from "react";
import { useTranslation } from "../../hooks/useTranslation";

const ProfilUtilisateur = () => {
  const { t } = useTranslation();
  return <div>ProfilUtilisateur</div>;
};

export default ProfilUtilisateur;
